server:
  port: 8086

spring:
  application:
    name: digital-factory-alumnos

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.scotiabank.digitalfactory.alumnos: DEBUG